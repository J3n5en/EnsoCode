{
  "name": "@enso/relay",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @enso/phone build && wrangler dev",
    "release": "pnpm --filter @enso/phone build && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250801.0",
    "typescript": "^7.0.2",
    "wrangler": "^4.127.0"
  }
}
