# 用量排行表展开全部

用量设置「按模型 / 按项目」排行默认只显示前 8 项，超过时无法查看其余行。

## 需求

- 行数超过默认上限时，表底提供「显示所有 / 收起」切换。
- 展开后渲染全部行；占比仍按全表 tokens，不因截断重算。
- 标题旁计数：折叠为「共 N 项，显示前 K 项」；展开为「共 N 项」。
- 不超过上限时不出现切换控件。

## 验收

- 23 行时默认 8 行 + 展开按钮；点开后 23 行 + 收起。
- 按项目表同样可用。
