# 实现

1. `src/shared/windowsLocalShell.ts`：类型 + `parseWindowsLocalShell`。
2. `sessionShell.ts` 接收 `preference`。
3. spawn-parent 白名单与 supervisor 下传。
4. settings store / IPC 登记字段 / General 下拉 / i18n / 搜索。
5. capability catalog + gateway + coverage fixture。
